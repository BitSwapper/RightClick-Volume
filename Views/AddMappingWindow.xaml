<Window x:Class="RightClickVolume.AddMappingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RightClickVolume"
        xmlns:vm="clr-namespace:RightClickVolume.ViewModels"
        mc:Ignorable="d"
        Title="Add/Edit UIA Name Mapping" Height="200" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" 
        ShowInTaskbar="False" WindowStyle="ToolWindow"
        d:DataContext="{d:DesignInstance Type=vm:AddMappingViewModel}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="UIA Name (Key):" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="UiaNameTextBox" Text="{Binding UiaName, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,5"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Target Process:" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="ProcessNameTextBox" Text="{Binding ProcessNameDisplay, Mode=OneWay}" IsReadOnly="True" VerticalAlignment="Center" Margin="0,5"/>
        <Button Grid.Row="1" Grid.Column="2" Content="Browse..." Command="{Binding BrowseProcessCommand}" Width="70" Margin="5,5,0,5" VerticalAlignment="Center"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,5,0,10" TextWrapping="Wrap" Foreground="Gray" FontStyle="Italic"
                   Text="Enter the UIA Name exactly as it appears or was detected. This name will be used as the key to find the selected Target Process."/>

        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="OK" Width="80" Margin="5,0" Command="{Binding OkCommand}" IsDefault="True"/>
            <Button Content="Cancel" Width="80" Margin="5,0" Command="{Binding CancelCommand}" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>